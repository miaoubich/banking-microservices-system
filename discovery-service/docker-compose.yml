services:
  discovery-service:
    build: .
    ports:
      - "8761:8761"
    environment:
      - EUREKA_INSTANCE_HOSTNAME=discovery-service
      - EUREKA_SELF_PRESERVATION=false
    networks:
      - banking-network

networks:
  banking-network:
    name: banking-network
    driver: bridge
